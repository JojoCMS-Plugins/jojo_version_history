This plugin add version history to any tables. By default it adds columns to the page table and the article table. See the install folder for examples of how to add this to more tables.

This plugin requires the dwoo template engine.